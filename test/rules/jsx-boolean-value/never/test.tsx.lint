function render(){
  return(
    <Foo readOnly = { true } />
         ~~~~~~~~~~~~~~~~~~~ [Value must be omitted for boolean attributes]
    <Foo readOnly={true} />
         ~~~~~~~~~~~~~~~ [Value must be omitted for boolean attributes]
    <Foo readOnly />
  );
}
